import 'package:flutter/material.dart';

class ConnexionValidate extends StatelessWidget {
  const ConnexionValidate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("ConnexionValidate")),
    );
  }
}
